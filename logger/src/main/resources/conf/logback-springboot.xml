<?xml version="1.0" encoding="UTF-8"?>
<configuration >
    <!--定义日志文件的存储地址 勿在 LogBack 的配置中使用相对路径-->
    <property name="LOG_HOME" value="logs"/>
    <!-- 日志前缀 -->
    <property name="LOG_NAME_PREFIX" value="dili-logger" />
    <!-- 文件输出格式 -->
    <property name="PATTERN" value="[${LOG_NAME_PREFIX}]%-12(%date) [%thread] %-5level %logger{50}  [%file:%line] %msg%n"/>

    <!-- 开发环境 -->
    <springProfile name="dev">
        <appender name="appender" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>${PATTERN}</pattern>
            </encoder>
        </appender>
        <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_HOME}/${LOG_NAME_PREFIX}.log</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <!--日志文件输出的文件名-->
                <fileNamePattern>${LOG_HOME}/%d{yyyy-MM-dd}/${LOG_NAME_PREFIX}_%i.log.zip</fileNamePattern>
                <!--日志文件保留天数(FileNamePattern中的%d 格式有关，如果yyyy-MM-dd 则是天数)-->
                <maxHistory>30</maxHistory>
                <!--日志文件最大的大小-->
                <maxFileSize>10MB</maxFileSize>
            </rollingPolicy>
            <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
                <pattern>[${LOG_NAME_PREFIX}-dev]%-12(%date) |-%-5level [%thread] %c [%L] -| %msg%n</pattern>
            </encoder>
        </appender>
        <logger name="org.springframework.web" level="INFO"/>
        <logger name="com.dili.logger" level="DEBUG"/>
        <jmxConfigurator/>
    </springProfile>

    <!-- 测试环境 -->
    <springProfile name="test">
        <!-- 按照配置动态生成日志文件 -->
        <appender name="FILE"  class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_HOME}/${LOG_NAME_PREFIX}.log</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <!--日志文件输出的文件名-->
                <fileNamePattern>${LOG_HOME}/%d{yyyy-MM-dd}/${LOG_NAME_PREFIX}_%i.log.zip</fileNamePattern>
                <!--日志文件保留天数(FileNamePattern中的%d 格式有关，如果yyyy-MM-dd 则是天数)-->
                <maxHistory>30</maxHistory>
                <!--日志文件最大的大小-->
                <maxFileSize>10MB</maxFileSize>
            </rollingPolicy>
            <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
                <!--格式化输出：%d表示日期，%thread表示线程名，%-5level：级别从左显示5个字符宽度%msg：日志消息，%n是换行符-->
                <pattern>%date [%thread] %-5level %logger{50}  [%file:%line] %msg%n</pattern>
            </encoder>
        </appender>
        <logger name="org.springframework.web" level="INFO"/>
        <logger name="com.dili.logger" level="INFO"/>
        <jmxConfigurator/>
    </springProfile>

    <!-- 灰度环境 -->
    <springProfile name="pre">
        <!-- 按照配置动态生成日志文件 -->
        <appender name="FILE"  class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_HOME}/${LOG_NAME_PREFIX}.log</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <!--日志文件输出的文件名-->
                <fileNamePattern>${LOG_HOME}/%d{yyyy-MM-dd}/${LOG_NAME_PREFIX}_%i.log.zip</fileNamePattern>
                <!--日志文件保留天数(FileNamePattern中的%d 格式有关，如果yyyy-MM-dd 则是天数)-->
                <maxHistory>30</maxHistory>
                <!--日志文件最大的大小-->
                <maxFileSize>10MB</maxFileSize>
            </rollingPolicy>
            <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
                <!--格式化输出：%d表示日期，%thread表示线程名，%-5level：级别从左显示5个字符宽度%msg：日志消息，%n是换行符-->
                <pattern>%date [%thread] %-5level %logger{50}  [%file:%line]  %msg%n</pattern>
            </encoder>
        </appender>
        <logger name="org.springframework.web" level="INFO"/>
        <logger name="com.dili.logger" level="INFO"/>
        <jmxConfigurator/>
    </springProfile>

    <!-- 生产环境 -->
    <springProfile name="prod">
        <!-- 按照配置动态生成日志文件 -->
        <appender name="FILE"  class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_HOME}/${LOG_NAME_PREFIX}.log</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <!--日志文件输出的文件名-->
                <fileNamePattern>${LOG_HOME}/%d{yyyy-MM-dd}/${LOG_NAME_PREFIX}_%i.log.zip</fileNamePattern>
                <!--日志文件保留天数(FileNamePattern中的%d 格式有关，如果yyyy-MM-dd 则是天数)-->
                <maxHistory>30</maxHistory>
                <!--日志文件最大的大小-->
                <maxFileSize>10MB</maxFileSize>
            </rollingPolicy>
            <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
                <!--格式化输出：%d表示日期，%thread表示线程名，%-5level：级别从左显示5个字符宽度%msg：日志消息，%n是换行符-->
                <pattern>%date [%thread] %-5level %logger{50}  [%file:%line]  %msg%n</pattern>
            </encoder>
        </appender>
        <logger name="org.springframework.web" level="INFO"/>
        <logger name="com.dili.logger" level="INFO"/>
        <jmxConfigurator/>
    </springProfile>

    <root level="info">
        <appender-ref ref="appender" />
        <appender-ref ref="FILE" />
    </root>

</configuration>